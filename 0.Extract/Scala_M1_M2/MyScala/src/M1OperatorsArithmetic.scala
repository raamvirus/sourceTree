object M1Operators {
  def main(args: Array[String]) {
    var a = 30; 
    var b = 10; 
    var c = 50; 
    var d = 50; 
    println("a + b = " + (a + b)); 
    println("a - b = " + (a - b)); 
    println("a * b = " + (a * b)); 
    println("b / a = " + (b / a)); 
    println("b % a = " + (b % a));
    println("c % a = " + (c % a));
  }
}