object M2_4LoopWhile {

     def main(args: Array[String]) {
      // Local variable declaration:
      var a = 10;

      // while loop execution
      while( a < 20 ){
         print( "Value of a: " + a );
       //  a = a + 1;
         a+=1;
      }
   }
}